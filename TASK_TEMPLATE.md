# tasks/task-0X-<slug>/README.md — 과제 카드 템플릿

> 이 템플릿을 과제별로 복사해 채운다. 앞부분은 **12항목 과제 카드**, 뒷부분은 **실행/재현 명령 + 완료 정의(DoD)** 다.

## 과제 카드 (12항목)
- **과제명 / 방향:** (예: 과제01 — 3D 점유 월드모델 · Spatial Intelligence)
- **1. 문제 설명:** 무엇이 왜 어려웠는지 + 무엇으로 어떻게 개선/돌파했는지(핵심 논문·기법).
- **2. 왜 어려운가:**
  - …
- **3. 관련 과제:** …
- **4. 핵심 질문(RQ):** …
- **5. 예시 시나리오:** …
- **6. 실험 구조:** 1) … 2) … 3) …
- **7. 실험 설계(표):**

  | 구성요소 | 축소 재현 설정 |
  |---|---|
  | 목표 태스크 | … |
  | 데이터 | … (subset mini) |
  | 모델 | … |
  | 지표 | … |
  | 비교군(기준선) | … |
  | 절제(ablation) | … |
- **8. 학습자가 배울 수 있는 점:**
  - …
- **9. 상세 설명:** 수행 절차·범위 축소 방법을 실제 수행 가능하게.
- **10. 최초 프롬프트:** (Claude Code/Codex에 던질 시작 프롬프트, 한국어)
- **11. 난이도:** 전반 / ① 데이터 취득 용이성 / ② 게이밍PC 구동 가능성(실사용 GPU·VRAM 기준).
- **12. 태그:** #… #…

## 대표 논문·라이선스
- **앵커 논문:** 저자(연도). 제목. 학회/저널, DOI/링크. SCI(E) 여부.
- **브리지 논문:** 저자(연도). 제목. 학회/저널, DOI/링크. SCI(E) 여부.
- **데이터 라이선스:** (예: nuScenes 비상업·등록 필요 / METR-LA 연구용 공개 등)
- **코드 라이선스:** (원 공개 구현의 라이선스)

## 재현 방법 (실제 명령)
```bash
# 0) 환경
conda env create -f ../../environment.yml   # 또는 pip install -r ../../requirements.txt

# 1) 데이터(소규모)
bash scripts/download_data.sh --subset mini

# 2) 스모크런(파이프라인 검증)
bash scripts/smoke.sh

# 3) 기준선 → 본 학습
python scripts/train.py --config config/baseline.yaml
python scripts/train.py --config config/base.yaml

# 4) 평가
python scripts/eval.py --config config/base.yaml --ckpt results/<run_id>/best.ckpt
```

## 결과 (실제 실행값만 기입)
| 모델 | 지표1 | 지표2 | 하드웨어 | 비고 |
|---|---|---|---|---|
| 기준선 | … | … | (GPU/VRAM) | |
| 본 모델 | … | … | | |

## 한계 / 미확인
- (돌리지 못했거나 검증 안 된 부분을 정직하게)

## 완료 정의 (DoD) 체크
- [ ] `smoke.sh` 통과 + 문서화된 단일 명령으로 학습 재현
- [ ] 기준선 대비 지표 표(results/, 실제 값)
- [ ] 시드 고정 + 환경 파일 존재
- [ ] 데이터·가중치 git 미포함 + download 스크립트 + 라이선스 명시
- [ ] 앵커/브리지 논문 인용(DOI·SCI(E) 여부)
- [ ] Colab/게이밍PC 구동 가능성 + 실제 하드웨어 기록
- [ ] 실패·미확인 정직하게 기술
- [ ] 비밀키·대용량 파일 커밋 없음
